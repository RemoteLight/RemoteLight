description: Scene not found
content:
  application/json:
    schema:
      type: object
      properties:
        exception:
          type: string
          nullable: true
          example: null
        message:
          type: string
          example: No scene with id $id
